#include "main.h"

/**
 * print_rev - affiche une chaine de caracteres en sens inverse
 * @s: chaine a afficher
 */
void print_rev(char *s)
{
	int i;

	i = 0;
	while (s[i] != '\0')
	{
		i++;
	}
	i--;
	while (i >= 0)
	{
		_putchar(s[i]);
		i--;
	}
	_putchar('\n');
}
